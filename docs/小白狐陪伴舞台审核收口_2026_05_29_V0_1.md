# 小白狐陪伴舞台审核收口 v0.1

项目：`ai-child`

用途：记录“小白狐动态陪伴页 UI 升级”阶段 3 的主控审核结论，并明确是否进入阶段 4。本文只针对陪伴舞台，不涉及完整 UI 重排。

更新时间：2026-05-29

---

## 0. 被审核提交

```text
7a89cd8abbc92c114b1824b06c2be26f2af83fb9
```

提交目标：

```text
feat: 小白狐陪伴舞台 - 柔和渐变背景、状态光晕、状态气泡
```

主控审核结论：

```text
阶段 3 可以收口；允许进入阶段 4。
```

阶段 4 进入方向：

```text
消息区和底部操作区重排。
```

但阶段 4 仍是 UI 层级收敛，不允许改业务状态机、prompt、家长日报或新增功能。

---

## 1. 已确认完成点

阶段 3 已完成：

```text
1. 新增 XiaobaohuCompanionStage；
2. 用陪伴舞台替换 AgentPanel 内部旧 glow + CartoonAgentView 结构；
3. 增加暖色渐变背景；
4. 增加基于 MascotState 的状态光晕；
5. 增加状态气泡；
6. 状态气泡短句符合主控口径；
7. 未直接重排消息区；
8. 未直接重排底部操作区；
9. 未改 prompt、家长日报、低压等待业务逻辑。
```

当前状态气泡：

```text
idle：小白狐在这里。
waiting_soft：想说的时候再说。
listening：正在听你说。
thinking：小白狐在想一想。
preparing_speech：小白狐在准备说。
speaking：不显示气泡。
image_viewing：小白狐正在看。
co_create：可以一起想一想。
paused：好，我们先停一下。
retry：这次没弄好，我们可以再试一次。
```

主控判断：

```text
可以接受。
```

---

## 2. 已知问题与处理方式

### 2.1 无真机验证

当前仍未完成 Android 真机验证。

处理方式：

```text
不阻塞阶段 4；阶段 4 完成后必须整体真机复测。
```

真机复测重点：

```text
1. 小白狐舞台是否过大或被裁切；
2. 状态气泡是否遮挡小白狐；
3. 动图播放是否流畅；
4. 横屏/平板是否压缩严重；
5. 低端设备是否卡顿；
6. paused 是否过早压掉 listening/speaking；
7. retry 是否不吓人；
8. co_create 是否不呈现奖励感。
```

### 2.2 dead code

开发端报告：

```text
foxGlowColorForPhase()
AgentReplyCarouselText
```

已不被调用但仍保留。

主控判断：

```text
不阻塞阶段 4；可后续低风险清理。
```

要求：

```text
阶段 4 不要顺手清理 dead code，除非它影响编译或测试。
```

### 2.3 预存测试失败

已知测试：

```text
ChatViewModelStreamTest.audioReadyUsesQueueWhenNotMuted
```

主控判断：

```text
仍作为 unrelated known issue，不混入阶段 4。
```

### 2.4 paused 优先级

当前：

```text
paused 排在 listening/speaking 前面。
```

主控判断：

```text
不阻塞阶段 4；阶段 4 不要改状态优先级。
```

后续真机如发现安全/隐私/睡前场景过早压掉朗读或录音，再单独开状态优先级修复任务。

---

## 3. 是否进入阶段 4

允许进入：

```text
阶段 4：消息区和底部操作区重排。
```

阶段 4 目标是：

```text
让消息区退到第二视觉层级；
让底部操作区按孩子真实操作优先级重排；
进一步靠近 image-2 效果图的陪伴页结构。
```

阶段 4 不得做：

```text
1. 不改业务状态机；
2. 不改 prompt；
3. 不改家长日报；
4. 不改小白狐资源；
5. 不新增玩法；
6. 不做拍图面板重设计；
7. 不做作品库/故事库；
8. 不修 unrelated audioReadyUsesQueueWhenNotMuted。
```

---

## 4. 阶段 3 收口结论

阶段 3 可以收口。

下一步进入阶段 4，但必须继续保持边界：

```text
只做儿童页视觉层级和操作区优先级收敛；
不要把阶段 4 变成新一轮全局体验大改。
```
